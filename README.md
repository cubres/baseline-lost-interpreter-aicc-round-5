# Baseline - Lost Interpreter | AICC Round 5

Deterministic baseline for the Lost Interpreter AICC Round 5 task.

The notebook expects these files in the repository root:

- `train.csv`
- `test.csv`

Run `baseline-lost-interpreter-aicc-round-5.ipynb` to validate the interpreter on `train.csv` and regenerate `submission.csv`.
